import { Routes } from '@angular/router';
import { LensComponent } from './lens/lens.component';

export const routes: Routes = [
    {path: "", component: LensComponent},
    {path: "lens", component: LensComponent}
];
